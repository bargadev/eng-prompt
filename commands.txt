Comando para executar python venv: 
. .venv/bin/activate
ou
source .venv/bin/activate

pip install -r ./requirements.txt

jupyter lab

python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
